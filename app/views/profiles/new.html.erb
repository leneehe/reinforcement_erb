<% fields = ['email', 'username', 'pin', 'website', 'address', 'alias']%>

<%= form_tag('/profiles') do %>

  <% fields.each do |field| %>
    <%= label_tag field, field%>
    <%= text_field_tag field, nil, class: 'fancy_form'  %>
  <% end %>

<%= submit_tag 'Enter' %>
<% end %>
